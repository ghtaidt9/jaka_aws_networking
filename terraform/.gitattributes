# Force LF line endings for cloud-init scripts and templates rendered into
# EC2 user_data, regardless of core.autocrlf on the checkout machine.
# A CRLF shebang line ("#!/bin/bash\r\n") breaks cloud-init parsing.
*.tpl text eol=lf
*.sh text eol=lf
